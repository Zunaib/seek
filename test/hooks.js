import React, {useEffect, useState} from 'react'


useEffect(() => {

}, [])


const hooksexample = () => {

  const [state, setstate] = useState(initialState)

  return (
    <>
    <input value={} onChange={} type="text" />
      
    </>
  )
}

export default hooksexample;

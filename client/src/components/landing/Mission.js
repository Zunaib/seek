import React from "react";

const Mission = (props) => (
  <section className="Misc">
    <h2>Our Mission</h2>
    <div>
      <p>
        We provide a platform for Camera based security driven environments that
        can help guards, security personnel and teams to detect Suspicious
        activities and objects.
      </p>
    </div>
  </section>
);

export default Mission;
